#########################################本地开发环境#########################################
spring:
  application:
  name: "scheduledemo"
  http:
    encoding:
      charset: "UTF-8"
      enabled: true
      force: true
  profiles:
    active: dev
server:
  port: 8085
  servlet:
    context-path: /

logging:
  config: "classpath:logback.xml"


